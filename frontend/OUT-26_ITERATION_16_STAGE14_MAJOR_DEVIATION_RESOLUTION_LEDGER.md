# OUT-26 Iteration 16 Stage 14 Major-Deviation Resolution Ledger

| Finding ID | Original Stage 13 severity | Stage 14 target | Files/symbols changed | Recovery action | Evidence | Final status | Remaining gap | Next prompt rule |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `ND-001` | `MAJOR` | reduce local page ownership | `frontend/src/components/Assets.tsx`; `frontend/src/components/shared/OperationalWorkspaceHooks.ts` | Moved overlay control and workspace view persistence into shared hooks/primitives; bulk anchored panel no longer locally absolutely positioned. | Shared hook + overlay controller wiring; desktop shell still renders correctly. | `PARTIAL` | report/map/compare shell branching still lives in `Assets.tsx`. | Push more shell ownership into golden slots without touching domain surfaces. |
| `ND-002` | `MAJOR` | replace ad hoc route/view persistence with closest approved contract | `frontend/src/components/Assets.tsx`; `frontend/src/components/shared/OperationalWorkspaceHooks.ts` | Added `useOperationalWorkspaceViewState(...)`, persisted working view state and active view id, kept accepted route grammar, and verified refresh-safe search/view restoration. | `/asset?search=FINANCE-S-001` persisted through refresh; display/views panels render from shared state path. | `PARTIAL` | still frontend-only persistence; no approved backend preference contract was introduced. | Decide whether the next slice stops at shared frontend persistence or migrates to an existing approved backend preference contract. |
| `ND-003` | `MAJOR` | isolate row action trigger | `frontend/src/components/Assets.tsx` | Guarded row-body click handling against button-origin targets and decoupled quick look from selection side effects. | Row-action evidence shows menu open, `quickLookOpen: false`, `selectedRows: 0`; row-body click still opens quick look. | `PASS` | none in current rendered pass. | Lock this behavior; do not revisit unless regression appears. |
| `ND-004` | `MAJOR` | shared golden bulk grammar | `frontend/src/components/Assets.tsx` | Replaced local absolute flyout with `OperationalAnchoredPanel` + shared flyout grammar and preserved asset-specific actions. | Bulk panel text and geometry show aligned grammar with preserved asset actions. | `PASS` | none in current slice. | Keep current bulk grammar stable while later slices address deeper decomposition only if required. |
| `ND-005` | `MAJOR` | usable `960x720` layout | `frontend/src/components/Assets.tsx` | Reduced wrap pressure in the command zone and made the lens bar scroll horizontally instead of stacking. | `960x720` evidence shows grid still visible with `visibleRowCount: 12`, Existing/Purged accessible, and toolbar controls readable. | `PARTIAL` | grid height improved from the Stage 13 collapse, but remains shorter than ideal. | Target command-area compaction only; preserve current top-right scope and visible rows baseline. |
