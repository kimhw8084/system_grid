import React, { useMemo, useState } from 'react'
import { useLocation, useNavigate } from 'react-router-dom'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { apiFetch } from '../api/apiClient'
import { AlertTriangle, Check, ChevronDown, ChevronRight, CircleHelp, Pin, Plus, Undo2 } from 'lucide-react'
import { BOARD_STATUSES, buildWorkRows, focusDisplayItems, parseTaskImportText, transitionLabel, treeGridAria, WORK_STATUSES } from './ProjectsWorkPlan.model'
import './ProjectsWorkPlan.css'

type Route = { scope: 'my-day' | 'work' | 'plan'; projectId?: string; board: boolean }

export function shouldUseProjectsWorkPlan(pathname: string, search = ''): Route | null {
  if (pathname === '/projects/my-day' || pathname === '/projects/my-day/') return { scope: 'my-day', board: false }
  const work = pathname.match(/^\/projects\/([^/]+)\/(work|plan)\/?$/)
  if (!work) return null
  const params = new URLSearchParams(search)
  return { scope: work[2] as 'work' | 'plan', projectId: decodeURIComponent(work[1]), board: params.get('layout') === 'board' }
}

const uuid = () => globalThis.crypto?.randomUUID?.() || `${Date.now()}-${Math.random()}`

async function jsonOrThrow(response: Response) {
  if (!response.ok) throw new Error(await response.text())
  return response.json()
}

function command(projectId: string, type: string, expected: Record<string, unknown>, payload: Record<string, unknown>) {
  const commandId = uuid()
  return apiFetch(`/api/v2/projects/${encodeURIComponent(projectId)}/commands`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', 'Idempotency-Key': commandId },
    body: JSON.stringify({ command_id: commandId, type, expected, payload }),
  }).then(jsonOrThrow)
}

function Header({ projectId, scope, board }: { projectId?: string; scope: Route['scope']; board: boolean }) {
  return <header className="p05-header"><div><p className="p05-eyebrow">Projects · {scope === 'my-day' ? 'Personal execution' : projectId ? 'Project workspace' : 'Work'}</p><h1>{scope === 'my-day' ? 'My day' : scope === 'plan' ? 'Plan' : 'Work'}</h1></div><nav aria-label="Work navigation"><a href="/projects">Portfolio</a><a href="/projects/my-day" aria-current={scope === 'my-day' ? 'page' : undefined}>My day</a>{projectId ? <><a href={`/projects/${projectId}/home`}>Home</a><a href={`/projects/${projectId}/work`} aria-current={scope === 'work' && !board ? 'page' : undefined}>Work</a><a href={`/projects/${projectId}/work?layout=board`} aria-current={scope === 'work' && board ? 'page' : undefined}>Board</a><a href={`/projects/${projectId}/plan`} aria-current={scope === 'plan' ? 'page' : undefined}>Plan</a></> : null}</nav></header>
}

function FocusSection({ response, projectId, onRefresh }: { response: any; projectId?: string; onRefresh: () => void }) {
  const items = focusDisplayItems(response)
  const queryClient = useQueryClient()
  const [message, setMessage] = useState('')
  const focusMutation = useMutation({ mutationFn: ({ type, item }: { type: string; item: any }) => {
    const id = uuid()
    return apiFetch('/api/v2/focus/commands', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Idempotency-Key': id }, body: JSON.stringify({ command_id: id, type, expected: {}, payload: { project_id: item.project_id, entity_kind: item.entity_kind, entity_id: item.entity_id } }) }).then(jsonOrThrow)
  }, onSuccess: () => { setMessage('Focus preference saved.'); queryClient.invalidateQueries({ queryKey: ['pv1-focus'] }); onRefresh() } })
  return <section className="p05-focus" aria-labelledby="focus-title"><div className="p05-section-heading"><div><p className="p05-eyebrow">{response?.scope === 'project' ? 'Project Focus' : 'Focus today'}</p><h2 id="focus-title">The next useful actions</h2><p>Deterministic recommendations from the same task graph used by Work and Board.</p></div><span className="p05-source">{response?.total ?? 0} actionable · {response?.engine_version || 'pv-focus-1'}</span></div>{message ? <p role="status" className="p05-success">{message}</p> : null}{!items.length ? <p className="p05-empty">No actionable work is assigned to you. Unscheduled work remains visible in Work.</p> : <ol className="p05-focus-list">{items.map((item: any) => <li key={item.id} className={item.pinned ? 'is-pinned' : undefined}><div className="p05-focus-main"><span className="p05-bucket">{item.bucket_label}</span><strong>{item.title}</strong><span>{item.project_name} · {item.due_context}</span><small>{item.why_here}</small></div><div className="p05-focus-actions"><button title="Why here?" aria-label={`Why here for ${item.title}`} onClick={() => setMessage(item.why_here)}><CircleHelp size={16} /></button><button aria-label={`${item.pinned ? 'Unpin' : 'Pin'} ${item.title}`} onClick={() => focusMutation.mutate({ type: item.pinned ? 'focus.unpin' : 'focus.pin', item })}><Pin size={16} /></button><a href={`/projects/${item.project_id}/work?panel=task&entity=${item.entity_id}`}>{item.primary_action}</a></div></li>)}</ol>}{message ? <small className="p05-live" aria-live="polite">{message}</small> : null}</section>
}

function WorkTree({ projectId, work, board, onRefresh }: { projectId: string; work: any; board: boolean; onRefresh: () => void }) {
  const queryClient = useQueryClient()
  const [expanded, setExpanded] = useState<Set<string>>(new Set())
  const [selected, setSelected] = useState<Set<string>>(new Set())
  const [newTitle, setNewTitle] = useState('')
  const [importText, setImportText] = useState('')
  const [importPreview, setImportPreview] = useState<any>(null)
  const [panelId, setPanelId] = useState<string | null>(null)
  const [message, setMessage] = useState('')
  const rows = useMemo(() => buildWorkRows(work?.items || [], expanded), [work?.items, expanded])
  const aria = useMemo(() => treeGridAria(work?.items || [], expanded), [work?.items, expanded])
  const refresh = () => { queryClient.invalidateQueries({ queryKey: ['pv1-work', projectId] }); queryClient.invalidateQueries({ queryKey: ['pv1-focus'] }); onRefresh() }
  const send = useMutation({ mutationFn: ({ type, expected, payload }: { type: string; expected: any; payload: any }) => command(projectId, type, expected, payload), onSuccess: refresh, onError: (error: Error) => setMessage(error.message) })
  const create = () => { if (!newTitle.trim()) return; send.mutate({ type: 'task.create', expected: { project_revision: work.project_revision, graph_revision: work.graph_revision }, payload: { title: newTitle.trim() } }); setNewTitle('') }
  const toggle = (id: string) => setExpanded((current) => { const next = new Set(current); next.has(id) ? next.delete(id) : next.add(id); return next })
  const transition = (task: any, status: string) => send.mutate({ type: 'task.transition', expected: { project_revision: work.project_revision, graph_revision: work.graph_revision, task_revision: task.revision }, payload: { task_id: task.id, to_status: status } })
  const bulk = (operation: string, value: unknown) => { const ids = [...selected]; if (!ids.length) return; send.mutate({ type: 'task.bulk', expected: { project_revision: work.project_revision, graph_revision: work.graph_revision, task_revisions: Object.fromEntries(ids.map((id) => [id, (work.items || []).find((item: any) => String(item.id) === id)?.revision])) }, payload: { task_ids: ids, operation, value } }); setSelected(new Set()) }
  const previewImport = async () => { const format = importText.includes('\t') ? 'tsv' : 'csv'; const response = await apiFetch(`/api/v2/projects/${encodeURIComponent(projectId)}/tasks/import/preview`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ text: importText, format }) }); setImportPreview(await jsonOrThrow(response)) }
  const applyImport = async () => { if (!importPreview?.valid) return; const id = uuid(); const response = await apiFetch(`/api/v2/projects/${encodeURIComponent(projectId)}/tasks/import`, { method: 'POST', headers: { 'Content-Type': 'application/json', 'Idempotency-Key': id }, body: JSON.stringify({ text: importText, format: importText.includes('\t') ? 'tsv' : 'csv', expected: { graph_revision: work.graph_revision } }) }); await jsonOrThrow(response); setImportPreview(null); setImportText(''); refresh() }
  const panel = (work.items || []).find((item: any) => String(item.id) === panelId)
  if (board) return <div className="p05-board-wrap"><div className="p05-toolbar"><button onClick={() => bulk('status', 'In progress')} disabled={!selected.size}>Bulk → In progress</button><button onClick={() => bulk('cancel', null)} disabled={!selected.size}>Cancel selected</button><span>{selected.size} selected</span></div><div className="p05-board">{BOARD_STATUSES.map((status) => <section key={status} aria-labelledby={`board-${status}`}><h3 id={`board-${status}`}>{status}</h3>{(work.items || []).filter((item: any) => item.status === status).map((task: any) => <article className="p05-card" key={task.id} draggable={false}><label><input type="checkbox" checked={selected.has(String(task.id))} onChange={() => setSelected((current) => { const next = new Set(current); next.has(String(task.id)) ? next.delete(String(task.id)) : next.add(String(task.id)); return next })} /> <span>{task.title}</span></label><small>{task.owner_id || 'Unassigned'} · {task.end_date || 'No finish'}</small><button onClick={() => setPanelId(String(task.id))}>Open task</button><label className="p05-move"><span>Move to…</span><select aria-label={`Move ${task.title} to`} value={task.status} onChange={(event) => transition(task, event.target.value)}>{WORK_STATUSES.map((value) => <option key={value}>{value}</option>)}</select></label></article>)}</section>)}</div></div>
  return <div className="p05-work-area"><div className="p05-toolbar"><label className="p05-inline-add"><span className="sr-only">New task title</span><input value={newTitle} onChange={(event) => setNewTitle(event.target.value)} onKeyDown={(event) => { if (event.key === 'Enter') create(); if (event.key === 'Escape') setNewTitle('') }} placeholder="Add task title and press Enter" /><button onClick={create}><Plus size={16} /> Add task</button></label><button onClick={() => bulk('status', 'Done')} disabled={!selected.size}>Bulk → Done</button><button onClick={() => bulk('cancel', null)} disabled={!selected.size}>Cancel selected</button><span>{work.items?.length || 0} tasks</span></div>{message ? <p className="p05-error" role="alert">{message}</p> : null}<div className="p05-import"><label>Paste CSV/TSV<input value={importText} onChange={(event) => setImportText(event.target.value)} placeholder="title,owner,status,start,finish,parent_key,progress" /></label><button onClick={previewImport} disabled={!importText.trim()}>Preview</button>{importPreview ? <span>{importPreview.count} rows · {importPreview.errors?.length || 0} errors <button onClick={applyImport} disabled={!importPreview.valid}>Confirm import</button></span> : null}</div><div className="p05-treegrid" role="treegrid" aria-label="Project work breakdown" aria-rowcount={aria.rowCount} aria-description={`Unfiltered task count ${aria.unfilteredCount}`} data-virtualized="true"><div role="row" aria-rowindex={1} className="p05-grid-row p05-grid-header"><span role="columnheader">Task</span><span role="columnheader">Owner</span><span role="columnheader">Status</span><span role="columnheader">Progress</span><span role="columnheader">Finish</span></div>{rows.map((row) => { const task = row.task; const id = String(task.id); const blocked = (work.blockers || []).some((item: any) => String(item.task_id) === id); return <div role="row" key={id} aria-rowindex={rows.indexOf(row) + 2} aria-level={row.depth} aria-posinset={row.posInSet} aria-setsize={row.setSize} aria-expanded={row.children.length ? row.expanded : undefined} tabIndex={0} className="p05-grid-row" onDoubleClick={() => setPanelId(id)}><span role="gridcell" className="p05-task-name" style={{ paddingLeft: `${(row.depth - 1) * 20}px` }}><input type="checkbox" aria-label={`Select ${task.title}`} checked={selected.has(id)} onChange={() => setSelected((current) => { const next = new Set(current); next.has(id) ? next.delete(id) : next.add(id); return next })} />{row.children.length ? <button aria-label={`${row.expanded ? 'Collapse' : 'Expand'} ${task.title}`} onClick={() => toggle(id)}>{row.expanded ? <ChevronDown size={15} /> : <ChevronRight size={15} />}</button> : <span className="p05-indent" />}{task.kind === 'Milestone' ? <span aria-label="Milestone">◆</span> : null}<button className="p05-link-button" onClick={() => setPanelId(id)}>{task.title}</button>{blocked ? <AlertTriangle size={14} aria-label="Blocked" /> : null}</span><span role="gridcell">{task.owner_id || 'Unassigned'}</span><span role="gridcell"><select aria-label={`Status for ${task.title}`} value={task.status} onChange={(event) => transition(task, event.target.value)}>{WORK_STATUSES.map((value) => <option key={value}>{value}</option>)}</select></span><span role="gridcell"><input aria-label={`Progress for ${task.title}`} type="number" min={0} max={99} value={task.progress} onChange={(event) => send.mutate({ type: 'task.update_fields', expected: { project_revision: work.project_revision, graph_revision: work.graph_revision, task_revision: task.revision }, payload: { task_id: task.id, progress: Number(event.target.value) } })} />%</span><span role="gridcell">{task.end_date || task.point_date || 'Unscheduled'}</span></div>})}</div>{panel ? <aside className="p05-task-panel" aria-label={`Task panel ${panel.title}`}><button onClick={() => setPanelId(null)}>Close</button><p className="p05-eyebrow">Task panel</p><h2>{panel.title}</h2><p>{transitionLabel(panel.status, (work.blockers || []).some((item: any) => String(item.task_id) === String(panel.id)))}</p><p>Owner: {panel.owner_id || 'Unassigned'}</p><p>Progress: {panel.progress}%</p><button onClick={() => transition(panel, panel.status === 'Done' ? 'In progress' : 'Done')}><Check size={15} /> {panel.status === 'Done' ? 'Reopen' : 'Mark done'}</button><p className="p05-muted">Routine progress updates do not require a note.</p></aside> : null}</div>
}

function PlanView({ projectId, plan, onRefresh }: { projectId: string; plan: any; onRefresh: () => void }) {
  const [resourceTitle, setResourceTitle] = useState('')
  const resourceMutation = useMutation({ mutationFn: () => command(projectId, 'resource.save', { project_revision: plan.source_revisions.project_revision }, { title: resourceTitle, resource_kind: 'General note', content: '', pinned: false }), onSuccess: () => { setResourceTitle(''); onRefresh() } })
  return <div className="p05-plan"><section><p className="p05-eyebrow">Brief</p><h2>Project brief</h2><dl className="p05-brief"><div><dt>Problem</dt><dd>{plan.brief?.problem || 'Not recorded'}</dd></div><div><dt>Objective</dt><dd>{plan.brief?.objective || 'Not recorded'}</dd></div><div><dt>In scope</dt><dd>{plan.brief?.in_scope || 'Not recorded'}</dd></div><div><dt>Out of scope</dt><dd>{plan.brief?.out_of_scope || 'Not recorded'}</dd></div><div><dt>Delivery acceptance</dt><dd>{plan.brief?.delivery_acceptance?.length || 'No'} criteria recorded</dd></div></dl></section><section><p className="p05-eyebrow">Milestones</p><h2>Milestones</h2><div className="p05-simple-list">{(plan.milestones || []).map((item: any) => <div key={item.id}><strong>{item.title}</strong><span>{item.point_date || 'Point date not recorded'} · {item.status}</span></div>)}{!plan.milestones?.length ? <p className="p05-empty">No milestone recorded.</p> : null}</div></section><section><p className="p05-eyebrow">Work breakdown</p><h2>Shared task graph</h2><p>Plan reads the same records as Work; use <a href={`/projects/${projectId}/work`}>Edit breakdown</a> to change them.</p><div className="p05-simple-list">{(plan.work_breakdown || []).slice(0, 20).map((item: any) => <div key={item.id}><strong>{item.title}</strong><span>{item.status} · {item.progress}%</span></div>)}</div></section><section><p className="p05-eyebrow">Architecture</p><h2>Architecture impact</h2><p>{plan.architecture?.assessment || 'Not assessed'}{plan.architecture?.rationale ? ` · ${plan.architecture.rationale}` : ''}</p></section><section><p className="p05-eyebrow">Risks & decisions</p><h2>Typed planning records</h2><div className="p05-simple-list">{(plan.governance || []).map((item: any) => <div key={item.id}><strong>{item.type} · {item.title}</strong><span>{item.state} · {item.owner_id || 'Unassigned'}</span></div>)}{!plan.governance?.length ? <p className="p05-empty">No risks, issues, assumptions, or decisions recorded.</p> : null}</div></section><section><p className="p05-eyebrow">Resources</p><h2>Project library</h2><div className="p05-resource-add"><input value={resourceTitle} onChange={(event) => setResourceTitle(event.target.value)} placeholder="Resource title" /><button onClick={() => resourceMutation.mutate()} disabled={!resourceTitle.trim()}>Add resource</button></div><div className="p05-simple-list">{(plan.resources || []).map((item: any) => <div key={item.id}><strong>{item.title}</strong><span>{item.resource_kind} · {item.scan_state}{item.pinned ? ' · Pinned' : ''}</span></div>)}{!plan.resources?.length ? <p className="p05-empty">No resources are linked yet.</p> : null}</div></section>{plan.guidance?.length ? <aside className="p05-guidance" aria-label="Planning guidance"><h2>Planning guidance</h2>{plan.guidance.map((item: any) => <a key={item.code} href={`#${item.fix}`}><AlertTriangle size={15} />{item.message}</a>)}</aside> : null}</div>
}

export default function ProjectsWorkPlan() {
  const location = useLocation(); const navigate = useNavigate(); const route = shouldUseProjectsWorkPlan(location.pathname, location.search); const queryClient = useQueryClient()
  const activeRoute = route || { scope: 'my-day' as const, board: false }
  const projectId = activeRoute.projectId
  const focus = useQuery({ queryKey: ['pv1-focus', projectId || 'all'], queryFn: () => apiFetch(`/api/v2/focus${projectId ? `?project_id=${encodeURIComponent(projectId)}` : ''}`).then(jsonOrThrow), enabled: Boolean(route), staleTime: 10_000 })
  const work = useQuery({ queryKey: ['pv1-work', projectId], queryFn: () => apiFetch(`/api/v2/projects/${encodeURIComponent(projectId!)}/work`).then(jsonOrThrow), enabled: Boolean(projectId && activeRoute.scope === 'work'), staleTime: 10_000 })
  const plan = useQuery({ queryKey: ['pv1-plan', projectId], queryFn: () => apiFetch(`/api/v2/projects/${encodeURIComponent(projectId!)}/plan`).then(jsonOrThrow), enabled: Boolean(projectId && activeRoute.scope === 'plan'), staleTime: 10_000 })
  const refresh = () => { queryClient.invalidateQueries({ queryKey: ['pv1-focus'] }); queryClient.invalidateQueries({ queryKey: ['pv1-work', projectId] }); queryClient.invalidateQueries({ queryKey: ['pv1-plan', projectId] }) }
  if (!route) return null
  return <main className="p05-page" data-workspace="projects" data-pv1-projects-route="true"><Header projectId={projectId} scope={activeRoute.scope} board={activeRoute.board} />{focus.isPending ? <p className="p05-state">Loading Focus…</p> : focus.isError ? <p className="p05-error" role="alert">Focus unavailable. Work remains available when the server reconnects.</p> : <FocusSection response={focus.data} projectId={projectId} onRefresh={refresh} />}{activeRoute.scope === 'work' ? (work.isPending ? <p className="p05-state">Loading work…</p> : work.isError ? <p className="p05-error" role="alert">Work unavailable. Retry to refresh the canonical task graph.</p> : <WorkTree projectId={projectId!} work={work.data} board={activeRoute.board} onRefresh={refresh} />) : null}{activeRoute.scope === 'plan' ? (plan.isPending ? <p className="p05-state">Loading plan…</p> : plan.isError ? <p className="p05-error" role="alert">Plan unavailable.</p> : <PlanView projectId={projectId!} plan={plan.data} onRefresh={refresh} />) : null}<button className="p05-back" onClick={() => navigate(projectId ? `/projects/${projectId}/home` : '/projects')}>Back to {projectId ? 'Project Home' : 'Portfolio'}</button></main>
}
