# OUT-26 Iteration 15 Stage 13 No-Deviation Ledger

| ID | category | status | severity | observed behavior | expected golden behavior | evidence | affected files/symbols | user impact | violates plug-and-play law | exact next prompt rule | implementation required |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `ND-001` | Page-level composition / local ownership | Open | MAJOR | Canonical `Assets.tsx` still owns major page orchestration for toolbars, overlays, report/compare/map branching, and local grid CSS | Golden shell should own page composition while Assets supplies domain content into approved slots | Source audit: `Assets.tsx` toolbar/floating panel/bulk/menu/style sections; summary matrix categories A and T | `frontend/src/components/Assets.tsx` | Page is not yet true plug-and-play golden even though it is visible | Yes | Move remaining page-ownership grammar behind approved shared adapters or classify the branch as an explicit domain slot; do not alter route lock or rich asset slots | Yes |
| `ND-002` | Route-level state / saved views persistence | Open | MAJOR | Workspace state and saved views still rely on `usePersistentJsonState` localStorage plus ad hoc URL params; browser history proved noisy | Operational workspace state should follow the approved backend-scoped workspace-state contract with sanitized restores | Source: `ASSET_SAVED_VIEW_STORAGE_KEY`, `usePersistentJsonState`; rendered URL evidence including invalid-param sanitization and noisy back-state restore | `frontend/src/components/Assets.tsx`, `frontend/src/components/shared/OperationalWorkspaceHooks.ts` | Search/view/tab state is not fully goldenized or durable across operators/devices | Yes | Adopt the approved workspace-state contract for saved views and workspace persistence; keep param sanitization and route lock intact | Yes |
| `ND-003` | Row actions | Open | MAJOR | Rendered row-action interaction surfaced row-action content and quick-look content together; source row-action trigger does not stop propagation | Row-action trigger should open only the row-action surface and must not also trigger row selection/quick look | Rendered row-action body sample; source action button in column def calls `openRowActionMenu` without `stopPropagation()` | `frontend/src/components/Assets.tsx` row action cell renderer | Row-action interaction is ambiguous and risks accidental panel stacking | Yes | Standardize row-action trigger semantics so row-action clicks never open quick look as a side effect | Yes |
| `ND-004` | Bulk actions | Open | MAJOR | Bulk state is still locally owned in `Assets.tsx` with bespoke menu grammar and action cards | Bulk action grammar should use the shared bulk-action contract/controller without bespoke page-owned flow | Rendered `2 ASSETS SELECTED` state; source bulk menu block under `showBulkMenu` | `frontend/src/components/Assets.tsx`, `frontend/src/components/shared/OperationalBulkContract.ts` | Bulk operations remain outside the no-deviation golden flow | Yes | Replace local bulk-menu ownership with the approved shared bulk-action grammar while preserving asset-specific actions | Yes |
| `ND-005` | Responsive layout | Open | MAJOR | At `960x720`, the command bar grew to `260px` tall and the treegrid shrank to `56px` tall | Medium-width layouts must preserve usable table height and orderly action overflow | Rendered responsive metrics in evidence file | `frontend/src/components/Assets.tsx`, shell slot composition under `OperationalWorkspaceShell` | Medium-width users lose usable table area and scanning quality | Yes | Fix medium-width layout so command/header wrapping does not starve the grid height | Yes |
| `ND-006` | Console / overlay identity | Open | MINOR | Repeated duplicate-key warnings continue under `AnimatePresence`, `OperationalWorkspaceShells.tsx`, and `Assets.tsx` | No repeated duplicate-key warnings in steady-state workspace rendering | Console inventory in rendered evidence | `frontend/src/components/Assets.tsx`, `frontend/src/components/shared/OperationalWorkspaceShells.tsx` | Risk of unstable overlay identity and animation behavior | Partly | Remove duplicate keys once major conformance recovery is complete; do not broaden into unrelated refactor | Yes |
| `ND-007` | Quick look | Closed | ACCEPTABLE DOMAIN SLOT | Quick look remains asset-specific and uses a rich asset-oriented panel | Asset-specific quick look is allowed if mounted inside approved shell/floating primitives | Rendered quick-look content; shared floating-panel styling | `frontend/src/components/Assets.tsx` `QuickLookPanel` | Preserves critical asset behavior | No | Preserve exactly; do not flatten into Monitoring-style dossier | No |
| `ND-008` | Map | Closed | ACCEPTABLE DOMAIN SLOT | Map is asset-domain-specific and remains separate from table/report flows | Asset map is allowed as a domain-owned slot | Rendered instructional map state and source `AssetMap` branch | `frontend/src/components/Assets.tsx` `AssetMap` | Preserves topology-specific workflow | No | Preserve map as asset-owned surface while keeping shell/header/table standard outside it | No |
| `ND-009` | Details/forms | Closed | ACCEPTABLE DOMAIN SLOT | Asset create/edit form remains domain-specific inside shared `WorkspaceModal` shell | Asset form body is allowed as a domain-owned slot | Rendered create modal; shared modal shell source | `frontend/src/components/Assets.tsx`, `frontend/src/components/shared/WorkspaceModal.tsx` | Preserves asset-specific fields and workflows | No | Preserve; only audit lifecycle guard behavior later if needed | No |
| `ND-010` | Relationships and rich panels | Closed | ACCEPTABLE DOMAIN SLOT | Relationships, security, secrets, hardware, monitoring panels remain in rich asset detail surfaces | Rich asset panels are allowed domain-owned slots | Source preservation evidence from `AssetDetailsView.tsx` and stage summaries | `frontend/src/components/assets/AssetDetailsView.tsx` | Preserves domain richness | No | Preserve; do not delete or genericize | No |
| `ND-011` | Dirty-state lifecycle proof | Open | UNKNOWN | Shared dirty-guard source exists, but rendered discard flow was not conclusively re-driven in this audit session | Dirty prompt should be rendered-proven before final lock claim | Source: `WorkspaceModal.tsx`; rendered proof incomplete | `frontend/src/components/shared/WorkspaceModal.tsx`, `frontend/src/components/Assets.tsx` | Lock-readiness proof remains incomplete for this path | Unknown | If major issues are fixed, run a focused rendered proof pass for asset dirty-close and service/network dirty-close | Maybe |

## Notes

- No BLOCKER finding was recorded in Stage 13.
- `FAIL` is driven by the presence of unresolved MAJOR deviations, not by missing visible baseline recovery.
