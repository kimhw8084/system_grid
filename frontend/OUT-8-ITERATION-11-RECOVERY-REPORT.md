# OUT-8 Iteration 11 Recovery Report

## Status

This report completes the missing recovery-report artifact required after OUT-8 Iteration 11.

## Scope

Artifact completion only.

## Source-change rule

No source code changes were made for this report-generation step.
No package, dependency, route, component, hook, styling, or configuration changes were made for this report-generation step.

## Recovery objective

Restore the repository from the Iteration 10 saved-view abstraction regression back to the verified Iteration 9 / Iteration 11 safe baseline behavior.

## Required recovered behavior

- `useOperationalSavedViews` must not be present unless explicitly reintroduced by a future approved prompt.
- `persistedViews` must not be present unless explicitly reintroduced by a future approved prompt.
- `persistedActiveViewId` must not be present unless explicitly reintroduced by a future approved prompt.
- `MONITORING_ACTIVE_VIEW_KEY` cleanup must remain present in `MonitoringGrid.tsx`.

## Commands and results

The command output section below was generated by this shell prompt.

### Removed saved-view abstraction guard

```bash
if command -v rg >/dev/null 2>&1; then rg -n 'useOperationalSavedViews|persistedViews|persistedActiveViewId' src || true; else grep -RInE 'useOperationalSavedViews|persistedViews|persistedActiveViewId' src || true; fi
```

```text
```


### Monitoring active-view localStorage cleanup guard

```bash
if command -v rg >/dev/null 2>&1; then rg -n 'removeItem\(MONITORING_ACTIVE_VIEW_KEY\)' src/components/MonitoringGrid.tsx; else grep -n 'removeItem(MONITORING_ACTIVE_VIEW_KEY)' src/components/MonitoringGrid.tsx; fi
```

```text
1199:      window.localStorage.removeItem(MONITORING_ACTIVE_VIEW_KEY)
1232:             window.localStorage.removeItem(MONITORING_ACTIVE_VIEW_KEY)
```


### Known shift-selection bug recorded but not fixed in this artifact prompt

```bash
if command -v rg >/dev/null 2>&1; then rg -n 'rowIndex\s*>=\s*start\s*&&\s*rowIndex\s*>=\s*end' src || true; else grep -RInE 'rowIndex[[:space:]]*>=[[:space:]]*start[[:space:]]*&&[[:space:]]*rowIndex[[:space:]]*>=[[:space:]]*end' src || true; fi
```

```text
```


### Row-menu opener/setter guard

```bash
if command -v rg >/dev/null 2>&1; then rg -n 'openRowActionMenu:\s*setRowActionMenu|openRowActionMenu\s*=\s*setRowActionMenu|openRowActionMenu:\s*\w*Set\w*|openRowActionMenu\s*=\s*\w*Set\w*' src || true; else grep -RInE 'openRowActionMenu:[[:space:]]*setRowActionMenu|openRowActionMenu[[:space:]]*=[[:space:]]*setRowActionMenu|openRowActionMenu:[[:space:]]*[[:alnum:]_]*Set[[:alnum:]_]*|openRowActionMenu[[:space:]]*=[[:space:]]*[[:alnum:]_]*Set[[:alnum:]_]*' src || true; fi
```

```text
```


### npm lint if available

```bash
node -e "const p=require('./package.json'); process.exit(p.scripts&&p.scripts.lint?0:2)" && npm run lint || true
```

```text
```


### npm typecheck if available

```bash
node -e "const p=require('./package.json'); process.exit(p.scripts&&p.scripts.typecheck?0:2)" && npm run typecheck || true
```

```text

> system-grid-frontend@1.2.4 typecheck
> tsc --noEmit

src/components/NetworkReal.tsx(955,22): error TS2345: Argument of type '{ item: any; }' is not assignable to parameter of type 'SetStateAction<{ item: any; point: { x: number; y: number; }; }>'.
  Property 'point' is missing in type '{ item: any; }' but required in type '{ item: any; point: { x: number; y: number; }; }'.
src/components/NetworkReal.tsx(2571,35): error TS2339: Property 'style' does not exist on type '{ item: any; point: { x: number; y: number; }; }'.
src/components/VendorsReal.tsx(539,22): error TS2345: Argument of type '{ item: any; }' is not assignable to parameter of type 'SetStateAction<{ item: any; point: { x: number; y: number; }; }>'.
  Property 'point' is missing in type '{ item: any; }' but required in type '{ item: any; point: { x: number; y: number; }; }'.
src/components/VendorsReal.tsx(1158,167): error TS2339: Property 'style' does not exist on type '{ item: any; point: { x: number; y: number; }; }'.
```


### npm build if available

```bash
node -e "const p=require('./package.json'); process.exit(p.scripts&&p.scripts.build?0:2)" && npm run build || true
```

```text

> system-grid-frontend@1.2.4 build
> vite build

vite v5.4.21 building for production...
transforming...
✓ 4246 modules transformed.
rendering chunks...
computing gzip size...
dist/index.html                     1.28 kB │ gzip:     0.65 kB
dist/assets/index-DcVYUiQQ.css    453.19 kB │ gzip:    70.60 kB
dist/assets/index-j1PNkm24.js   4,723.72 kB │ gzip: 1,235.62 kB

(!) Some chunks are larger than 500 kB after minification. Consider:
- Using dynamic import() to code-split the application
- Use build.rollupOptions.output.manualChunks to improve chunking: https://rollupjs.org/configuration-options/#output-manualchunks
- Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.
✓ built in 5.28s
```


## Artifact decision

This report was generated to satisfy the missing Iteration 11 recovery-report artifact.
This step intentionally does not fix shift-selection. Under Prompt Generation Law v2.0, shift-selection remains the next high-priority cross-workspace bug after this artifact loop is closed by zip review.

## Lesson preserved

IF required report is missing twice, THEN switch to command-only shell creation, NOT another prose report prompt.
