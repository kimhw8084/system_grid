# OUT-14 External Acceptance Matrix

## Status

This matrix is the binding acceptance surface for Run 8 implementation and lock review.

Implementation stages used here:

- `S1` External baseline + gap map
- `S2` shell/grid contract wiring
- `S3` actions/panels/modal contract wiring
- `S4` lifecycle + import/export + diagnostics proof
- `S5` lock candidate review

| Area | Shared contract source | External current behavior | Required Run 8 behavior | Domain-specific preservation requirement | Deployment acceleration value | Risk if skipped | Proof method | PASS criteria | PARTIAL criteria | FAIL/WORSE trigger | Implementation stage |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Shell/header/command bar | `OperationalWorkspaceShells.tsx`, `OperationalWorkspace.ts`, `SettingsStandards.tsx` | External uses shared shell, header action zone, command slots, and scope switch | Keep External on shared shell without local shell drift | Preserve `External Intelligence`, scope counts, and three-scope model including `links` | `P0` | Future workspaces copy a false standard or a one-off shell | source diff + runtime visual compare vs Monitoring | Same shell grammar as Monitoring, no local shell fork | Mostly shared shell but with harmless local deviations under review | bespoke header/action layout or local shell copy | `S1-S2` |
| Search/filter/saved/display/action placement | `OperationalWorkspaceShells.tsx`, `SettingsStandards.tsx` | Search, views, display, import, filters, activity, compare, bulk, add are in shared slots | Preserve placement contract and keep entity-tab vs link-tab filter swap truthful | Preserve External-specific filter sets and action semantics | `P0` | Command-bar spread pattern becomes ambiguous | source diff + manual toolbar audit | Placement matches shared grammar and link-specific filters replace entity-only filters | shared placement intact but one secondary control needs cleanup | action placement becomes page-local or entity-only filters leak into links tab | `S1-S2` |
| Table layout and visual rhythm | `OperationalDataGrid.tsx`, `OperationalGridStandard.tsx`, `OperationalGridSizing.ts` | External uses shared raw/grouped grids and shared utility rhythm | Maintain shared row geometry, utility columns, grouped shells, empty/error surfaces | Preserve External columns and labels | `P0` | Users see External as an old page, not the golden system | source diff + visual comparison notes | Grid feels like same operating system as Monitoring while columns remain External-owned | minor visual drift with no contract break | local CSS table imitation or custom utility geometry | `S1-S2` |
| Sorting/filtering/selection/bulk behavior | `OperationalGridInteractions.ts`, `OperationalBulkContract.ts`, prior OUT-10 proofs | External uses shared selection scope, grouped/raw selection reset, shared bulk strip | Preserve native-feeling selection and explicit bulk scope | Preserve External bulk operations and purge restrictions | `P0` | Bulk behavior becomes non-standard and hard to spread | existing tests + manual selection checklist | click replace, cmd/ctrl toggle, shift range, grouped/raw reset, bulk count all hold | one low-risk selection edge unproven manually | hidden-scope bulk action or stale selection crosses scopes | `S1-S3` |
| Row action menu | `OperationalRowActionMenu.tsx`, `OperationalRowActionGeometry.ts`, `OperationalWorkspaceShells.tsx` | External uses shared row action menu shell with entity vs link sections | Keep shared row-action shell and geometry | Preserve External entity/link action differences, purge truth, follow actions | `P0` | Future migrations cannot trust row-action pattern reuse | source diff + right-click/manual row-action audit | shared row-action shell with correct External actions for each row type | shell shared but one action label/section needs repair | row actions reimplemented locally or External actions lost | `S3` |
| Right-click behavior | `OperationalGridInteractions.ts` | External suppresses native menu and opens shared context surface | Keep right-click semantics identical to shared contract | Preserve link-row vs entity-row targeting | `P0` | old browser menu or broken contextual action flow returns | manual browser validation | native menu suppressed and correct shared menu opens at cursor | inconsistent but recoverable positioning | right-click opens wrong record or browser menu | `S3` |
| Floating panels | `OperationalWorkspaceShells.tsx`, overlay hooks from `OperationalWorkspaceHooks.ts` | Views, display, bulk, and row action use shared overlay system | Maintain mutual exclusion, outside-click, Escape, and anchored placement | Preserve External unsaved-view draft guard and panel contents | `P0` | spreadable panel contract becomes unreliable | source diff + manual overlay arbitration checklist | only one panel active, close guards still truthful | one non-blocking positioning issue remains | page-local panel implementation or overlay collisions | `S3` |
| Add/Edit modal behavior | `WorkspaceModal.tsx`, `OperationalFormContracts.ts` | External edit/create uses shared modal shell and dirty-state wiring | Preserve shared modal shell, footer, maximize, and dirty-close law | Preserve External form body, field semantics, and backend error mapping | `P0` | CRUD standard drifts and future workspace migration cost rises | source diff + modal checklist | shared shell fully intact with External form preserved | shared shell intact but one footer/header detail needs cleanup | local modal shell or External form semantics lost | `S3` |
| Detail window behavior | `WorkspaceModal.tsx`, `OperationalWorkspaceHooks.ts`, `WorkspaceShareHeader.tsx` | Detail deep-links and detail modal use shared shell | Keep detail route and shell contract | Preserve External detail sections, diagnostics, secrets, contact and dependency display | `P0` | deep-link contract stops being reusable | manual detail-route validation | `/external?id=...` opens correct detail and transitions to edit/link cleanly | detail opens but one transition is brittle | route broken, wrong record opens, or detail body regresses | `S3-S5` |
| Compare modal behavior | `WorkspaceModalShells.tsx`, `WorkspaceModal.tsx` | External compare uses shared compare shell | Keep compare shell standardized | Preserve External compare fields and copy exactly | `P0` | compare becomes visually similar but semantically wrong | source diff + compare checklist | shared shell with External-specific compare content preserved | shell shared but one field set needs correction | compare content genericized or selection rules regress | `S3` |
| Link/dependency forms | `WorkspaceModal.tsx`, `OperationalDependencyGuard.ts` | Link form uses shared modal shell and local domain logic | Keep shared shell, dirty-close, and correct dependency semantics | Preserve link identity, uniqueness, device/service targeting, purpose, protocol, port, host/path semantics | `P0` | External’s distinctive workflow is damaged by genericization | backend/frontend tests + manual link flow | link create/edit/delete still truthfully matches backend semantics | shell shared but some link dirty-state proof still pending | link flow simplified, fields lost, or dependency truth broken | `S3-S5` |
| Dirty-state/unsaved-change behavior | `OperationalFormContracts.ts`, `WorkspaceModal.tsx` | Entity form is on shared dirty hook; link form still feeds dirty state into shared modal via local logic; saved-view draft close guard exists | Preserve dirty-close behavior across forms, detail transitions, and views panel | Preserve External-specific modal close flows and saved-view draft handling | `P0` | silent data loss | unit tests + manual unsaved-change checklist | no silent discard in entity form, link form, or saved-view draft flow | one path relies on manual proof only | any silent discard path | `S3-S5` |
| Lifecycle/data states | `OperationalDataState.ts`, `OperationalDataGrid.tsx`, OUT-9 proof | External uses shared loading/empty/error/data-state resolver for entities and links | Keep shared lifecycle/data-state surface and diagnostics behavior | Preserve correct entity-vs-link wording and endpoint detail | `P0` | users lose trust in shared failure/empty-state system | source diff + forced-failure manual checks | loading, empty, filtered, error, and diagnostic pill/modal remain truthful | shared surface intact but one label drift remains | bespoke lifecycle state or wrong diagnostic endpoint truth | `S4` |
| External import/export contract | `OperationalImportExport.ts`, `OperationalImportModal.tsx`, `import_engine.py`, OUT-13 proof | External import modal uses `external_entities`; snapshot export uses manifest-backed `downloadOperationalImportFile` | Keep Run 7 contract true end-to-end | Preserve profile, schema version, snapshot scope, filename pattern, and round-trip behavior | `P0` | deployment-safe file flow is lost, blocking External lock | source diff + diagnostics + automated tests + runtime report | manifest, CSV, headers/fallback, preview, and user-visible export action all align | manifest fallback works but one environment layer is degraded | viewport CSV regression, contract mismatch, or preview failure | `S4-S5` |
| External route behavior | `App.tsx`, `OperationalWorkspaceHooks.ts` | `/external` route exists and detail deep-link is wired | Keep route behavior stable and lock-specific | Preserve entity-detail semantics and archived-scope route restoration | `P0` | shareable workspace contract becomes unreliable | source diff + manual deeplink check | route opens External and valid `id` opens matching detail | route works but archived/link edge remains unproven | route ambiguity, broken deeplink, or wrong scope restore | `S4-S5` |
| External-specific schema/columns | `External.tsx`, backend schema/API responses | External owns entity/link columns and metadata-derived displays | Keep schema and columns domain-owned while rehosted in shared grid shell | Preserve External field identity and labels | `P0` | generic shell strips domain value | source diff + inventory cross-check | all lock work leaves External fields intact | one non-critical field display needs follow-up | field set removed or renamed without proof | `S1-S5` |
| External-specific endpoints/mutations | `backend/app/api/intelligence.py`, `External.tsx` | External CRUD, link, secret, restore, purge semantics are domain-specific | No endpoint redesign inside Run 8 | Preserve exact endpoint semantics and dependency truth | `P0` | broadening into backend redesign breaks lock scope | source audit + backend tests | existing endpoint semantics still satisfy External UI | one edge case needs follow-up but contract holds | backend redesign or semantic drift inside Run 8 | `S1-S5` |
| Diagnostics/readiness evidence | `externalExportDiagnostics.ts`, `systemDiagnostics.ts`, `runtime_diagnostics.py`, OUT-13 docs | Diagnostics source exists for export contract and readiness summary | Keep diagnostics able to evaluate External file flow in real environment | Preserve External-specific profile/schema truth in reports | `P0` | lock depends on guesswork instead of evidence | runtime report + copied diagnostics review | Settings/System Diagnostics can still report External contract truth clearly | report available but missing one evidence layer | no diagnostics proof or misleading report | `S4-S5` |
| Accessibility/keyboard interaction | shared shell/modal primitives + current app behavior | Some keyboard behavior is inherited, but External lock evidence is still mostly manual | Validate key keyboard paths relevant to lock | Preserve External action availability under keyboard paths where supported | `P1` | lock may pass while keyboard behavior drifts | manual checklist + any existing test evidence | Escape, focus trapping, modal close guards, and command triggers behave acceptably | minor edge remains documented for follow-up | keyboard path breaks critical action flow or silent close guard | `S4-S5` |
| Regression guardrails | repo tests, proof docs, zip review process | Existing frontend/backend tests cover External CRUD/link/import-export slices | Expand evidence set without pretending build alone is enough | Preserve External behavior while protecting shared-contract spreadability | `P0` | future runs repeat investigation or miss regressions | test run summary + zip review + lesson learned | tests, manual checks, diagnostics, and zip review align before lock | some proof exists but not enough to lock | lock claimed from build alone or without review packet | `S5` |
| Saved views and persisted workspace state | `External.tsx`, `OperationalWorkspace.ts`, `SettingsStandards.tsx` | `sanitizeExternalViewConfig` collapses `activeTab` to `active|deleted`, dropping `links` from restored views | Preserve `links` as a first-class saved/restored workspace mode | Preserve tab-specific filters and grouped/raw state under `links` | `P0` | External still behaves like a partial one-off consumer | source diff + refresh/apply-view manual check | saved/restored views round-trip `links` exactly | saved view mostly works but one restore edge remains | `links` state still dropped or mis-restored | `S2-S5` |
